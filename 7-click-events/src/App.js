// click event = An interaction when a user clicks on a specific element. We can respond to clicks by passing a callback
// function to the onClick prop of the element we want to listen for clicks on.

import Button from "./Button.jsx";
import ProfilePicture from "./ProfilePicture.jsx";

function App() {
  return(<ProfilePicture/>);
}

export default App;
